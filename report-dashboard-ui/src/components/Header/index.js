// React Import
import React from 'react';


const PageHeading = ({ pgHead }) => {
    return (
        <div className="alphagrep-page-heading">

            {pgHead}
        </div>
    );
};

export default PageHeading;
